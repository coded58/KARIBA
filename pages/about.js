import React from "react";

const about = () => {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        flex: 7,
      }}>
      <Header link='Dashboard' />
    </div>
  );
};

export default about;
